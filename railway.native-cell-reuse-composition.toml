[build]
buildCommand = "python -m unittest -v tests.test_native_cell_reuse_composition_v1 tests.test_native_cell_reuse_composition_adversarial_v1 tests.test_native_cell_reuse_composition_scale_v1"

[deploy]
startCommand = "python -c \"import time; print('native-cell-reuse-composition focused passed'); time.sleep(3600)\""
restartPolicyType = "NEVER"
