[build]
buildCommand = "python -m unittest -v tests.test_native_decision_realities_execution_v1"

[deploy]
startCommand = "python -c \"import time; print('native-decision execution diagnosis passed'); time.sleep(3600)\""
restartPolicyType = "NEVER"
