Temporary bootstrap assets for adversarial validation only. The workflow resets to the exact pinned main commit, proves the pre-fix Disk TOCTOU and Int64 backend divergence, applies the compressed six-file patch, runs the full suite and CI gates, and pushes only the verified final commit to agent/security-disk-toctou-int64-final. These bootstrap files must never be merged into main.
