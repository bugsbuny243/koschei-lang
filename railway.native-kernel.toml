[build]
buildCommand = "python -m unittest -v tests.test_native_kernel_v1 tests.test_native_kernel_adversarial_v1"

[deploy]
startCommand = "python -c \"import time; print('native-kernel redteam passed'); time.sleep(3600)\""
restartPolicyType = "NEVER"
