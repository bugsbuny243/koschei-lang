[build]
builder = "RAILPACK"

[deploy]
startCommand = "python tools/serve_pi_production_v1.py"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
