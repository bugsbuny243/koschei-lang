diff --git a/koschei/mir.py b/koschei/mir.py
index b75bd76..0000000 100644
--- a/koschei/mir.py
+++ b/koschei/mir.py
@@ -14,7 +14,7 @@ from __future__ import annotations
-from dataclasses import asdict, dataclass
+from dataclasses import asdict, dataclass
@@ -24,6 +24,8 @@ from .mir_ir import (
     MirAstFallback,
     MirBasicBlock,
+    MirJump,
+    MirBranch,
@@ -35,7 +37,7 @@ from .typed_hir import TypedHIRReport
-MIR_VERSION = 2
+MIR_VERSION = 3
@@ -45,6 +47,15 @@ class MirIntegrityError(Exception):
 
+@dataclass(frozen=True, slots=True)
+class MirResources:
+    basic_blocks: int
+    instructions: int
+    ast_fallbacks: int
+    backward_edges: int
+    self_recursive: bool
+
+
 @dataclass(frozen=True, slots=True)
 class MirParameter:
@@ -56,6 +67,7 @@ class MirFunction:
     return_type: TypeNode
     calls: tuple[str, ...]
     effects: tuple[str, ...]
+    resources: MirResources
     declaration: FunctionDeclaration
     blocks: tuple[MirBasicBlock, ...]
@@ -110,6 +122,11 @@ class MirGraph:
             for module in self.modules.values():
                 for function in module.functions:
                     validate_blocks(function.blocks)
+                    expected_resources = _resource_contract(
+                        function.name, function.calls, function.blocks
+                    )
+                    if function.resources != expected_resources:
+                        raise ValueError(f"resource contract mismatch for {module.name}.{function.name}")
                 expected_effects = infer_effects(module.program)
@@ -173,6 +190,7 @@ def _module_contract(module: MirModule) -> dict[str, Any]:
                 "return": render_type(function.return_type),
                 "calls": list(function.calls),
                 "effects": list(function.effects),
+                "resources": asdict(function.resources),
                 "blocks": [block_contract(block) for block in function.blocks],
@@ -230,6 +248,32 @@ def _fingerprint(root: str, modules: Mapping[str, MirModule]) -> str:
     return hashlib.sha256(encoded).hexdigest()
 
+
+def _resource_contract(
+    name: str,
+    calls: tuple[str, ...],
+    blocks: tuple[MirBasicBlock, ...],
+) -> MirResources:
+    fallbacks = sum(
+        1
+        for block in blocks
+        for instruction in block.instructions
+        if isinstance(instruction, MirAstFallback)
+    )
+    backward_edges = 0
+    for block in blocks:
+        terminator = block.terminator
+        if isinstance(terminator, MirJump) and terminator.target <= block.id:
+            backward_edges += 1
+        elif isinstance(terminator, MirBranch):
+            backward_edges += int(terminator.then_block <= block.id)
+            backward_edges += int(terminator.else_block <= block.id)
+    return MirResources(
+        basic_blocks=len(blocks),
+        instructions=sum(len(block.instructions) for block in blocks),
+        ast_fallbacks=fallbacks,
+        backward_edges=backward_edges,
+        self_recursive=name in calls,
+    )
+
 
 def lower_module(module: Any, typed_report: TypedHIRReport) -> MirModule:
@@ -245,6 +289,11 @@ def lower_module(module: Any, typed_report: TypedHIRReport) -> MirModule:
             function_type(declaration, declaration.return_type),
             effect_contracts[declaration.name][0],
             effect_contracts[declaration.name][1],
+            _resource_contract(
+                declaration.name,
+                effect_contracts[declaration.name][0],
+                lower_function_blocks(declaration, typed_report),
+            ),
             declaration,
             lower_function_blocks(declaration, typed_report),
@@ -307,6 +356,7 @@ def to_dict(mir: MirGraph) -> dict[str, Any]:
                         "return": render_type(function.return_type),
                         "calls": list(function.calls),
                         "effects": list(function.effects),
+                        "resources": asdict(function.resources),
                         "blocks": [
 diff --git a/tests/test_resource_contracts_v5.py b/tests/test_resource_contracts_v5.py
new file mode 100644
--- /dev/null
+++ b/tests/test_resource_contracts_v5.py
@@ -0,0 +1,118 @@
+from __future__ import annotations
+
+from dataclasses import replace
+from pathlib import Path
+from tempfile import TemporaryDirectory
+import unittest
+
+from koschei.mir import MIR_VERSION, MirIntegrityError, require_mir, to_dict
+from koschei.modules import check_graph, load_graph
+
+
+SOURCE = '''
+fn countdown(value: Int) -> Int {
+    if value == 0 {
+        return 0
+    }
+    return countdown(value - 1)
+}
+
+fn main() {
+    let mut value = 3
+    while value > 0 {
+        value = value - 1
+    }
+    println("resource contract ready")
+}
+'''
+
+
+class ResourceContractsV5Tests(unittest.TestCase):
+    def graph(self):
+        directory = TemporaryDirectory()
+        path = Path(directory.name) / "main.ks"
+        path.write_text(SOURCE, encoding="utf-8")
+        graph = load_graph(path)
+        check_graph(graph)
+        self.addCleanup(directory.cleanup)
+        return graph
+
+    def test_mir_schema_is_v3(self):
+        self.assertEqual(MIR_VERSION, 3)
+
+    def test_resource_contract_is_visible(self):
+        payload = to_dict(require_mir(self.graph()))
+        functions = {item["name"]: item for item in payload["modules"][0]["functions"]}
+        countdown = functions["countdown"]["resources"]
+        main = functions["main"]["resources"]
+        self.assertTrue(countdown["self_recursive"])
+        self.assertGreater(main["backward_edges"], 0)
+        self.assertGreater(main["instructions"], 0)
+
+    def test_forged_resource_contract_is_rejected(self):
+        mir = require_mir(self.graph())
+        module = mir.root_module
+        function = module.functions[0]
+        forged_resources = replace(function.resources, instructions=999999)
+        forged_function = replace(function, resources=forged_resources)
+        forged_module = replace(module, functions=(forged_function,) + module.functions[1:])
+        forged_modules = dict(mir.modules)
+        forged_modules[mir.root] = forged_module
+        forged = replace(mir, modules=forged_modules)
+        with self.assertRaises(MirIntegrityError):
+            forged.assert_sealed()
+
+    def test_resource_contract_is_deterministic(self):
+        first = to_dict(require_mir(self.graph()))
+        second = to_dict(require_mir(self.graph()))
+        self.assertEqual(first["fingerprint"], second["fingerprint"])
+        self.assertEqual(first["modules"][0]["functions"], second["modules"][0]["functions"])
+
+
+if __name__ == "__main__":
+    unittest.main()
+diff --git a/examples/resource_contracts.ks b/examples/resource_contracts.ks
new file mode 100644
--- /dev/null
+++ b/examples/resource_contracts.ks
@@ -0,0 +1,17 @@
+fn countdown(value: Int) -> Int {
+    if value == 0 {
+        return 0
+    }
+    return countdown(value - 1)
+}
+
+fn main() {
+    let mut value = 3
+    while value > 0 {
+        value = value - 1
+    }
+    println("resource contract ready")
+}
